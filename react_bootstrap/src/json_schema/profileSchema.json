{
    "type": "object",
    "properties": {
      "name": {
        "type": "string",
        "minLength": 1,
        "description": "Full Name"
      },
      "email": {
        "type": "string",
        "format": "email",
        "description": "Email Address"
      },
      "password": {
        "type": "string",
        "minLength": 6,
        "description": "Password"
      },
      "age": {
        "type": "integer",
        "minimum": 1,
        "description": "Age"
      }
    },
    "required": ["name", "email", "password", "age"]
  }
  